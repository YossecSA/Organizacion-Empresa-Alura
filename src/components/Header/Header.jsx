import React from "react";
import "./Header.css"
const Header = () => {
    return (
        <header className="header">
            <img src="/img/header.png" alt="Header de la imagen" />
        </header>
    );
};

export default Header;
